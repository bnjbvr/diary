extends loggedin

block sub
    .page-header
        h1 Global feed

    table(class="table table-striped")
        thead
            tr
                th Article
                th Last update
        tbody
            for e in essays
                tr
                    td
                        a(href=e.readLink)=e.title
                    td=formatDate(e.pubDate)
