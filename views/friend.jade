extends loggedin

block sub
    if profile
        .page-header
            h1=profile.name
            p=profile.bio

    .hero-unit
        h1=essay.title
        p
            | !{essay.summary}
    .container
        .row
            | !{essay.content}

