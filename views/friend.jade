extends loggedin

block sub
    #central
        if profile
            .page-header
                h1=profile.name
                p=profile.bio

        .hero-unit
            h1=essay.title
            p
                | !{essay.summary}
        p
            | !{essay.content}

