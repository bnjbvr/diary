extends loggedin

block sub
    .page-header
        h1 Essays

    table(class="table table-striped")
        thead
            tr
                th Article
                th Read link
                th Last update
                th Delete
        tbody
            for e in essays
                tr
                    td
                        a(href='/edit/' + e.id)
                            =e.content.title
                    td
                        a(href=e.readLink) Read link
                    td=formatDate(e.published_at)
                    td
                        a(href='/del/' + e.id).btn Delete
